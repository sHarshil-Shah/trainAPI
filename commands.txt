sudo yum install git-all
git clone https://github.com/sHarshil-Shah/trainAPI.git

/*
sudo yum install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o docker-archive-keyring.gpg

 echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt-get install docker-ce docker-ce-cli containerd.io
*/

sudo yum install docker
sudo service docker start
cd /home/ec2-user/trainAPI/demo
sudo chmod 666 /var/run/docker.sock
docker build -t trainAPI .